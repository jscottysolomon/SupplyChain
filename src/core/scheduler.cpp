#include "scheduler.hpp"

#include "truck.hpp"
#include "traffic.hpp"

void RouteScheduler::SetUp() {

  // commander_->ForEachTruck([&](int id, Truck& truck){
  //   for(int junc_id: truck.GetDynamicSchedule()) {
  //     Junction* junc = commander_->GetJunction(junc_id);
  //     if(junc && junc->GetFactory()) {
  //       CreatePlan(&truck,junc->GetFactory());
  //     }
  //   }
    
  // }); 

}